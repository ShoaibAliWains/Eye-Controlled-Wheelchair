# OpenCV for Image Processing (Direct Blob Tracking)
opencv-python

# Numpy for mathematical arrays and thresholding
numpy

# Raspberry Pi GPIO for Motor Control
RPi.GPIO

# NOTE: For Raspberry Pi Camera Module v3, 
# 'picamera2' must be installed via apt-get, not pip:
# sudo apt install python3-picamera2
